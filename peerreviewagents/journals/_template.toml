# Journal profile template. Copy to <slug>.toml and fill in what you know.
# Only `name` is required; leave unknown fields unset rather than guessing.
# See README.md for field meanings.

name = ""                  # exact venue name, e.g. "Nature Methods"
aliases = []               # ["Nat. Methods"]
publisher = ""             # publisher / society
field = ""                 # primary discipline, e.g. "bioinformatics"

# --- Reputation (record the year; these drift) ---
impact_factor = 0.0
impact_factor_year = 2025
acceptance_rate = ""       # e.g. "~8%"

# --- Audience & scope ---
audience = ""
description = ""           # 1-3 sentences on what the journal publishes
scope = ""                 # what is in / out of scope

# --- Submission limits (0 or unset = no hard limit) ---
max_words = 0
abstract_max_words = 0
max_figures = 0
max_references = 0

# --- Free-text author/reviewer guidance ---
guidelines = """
Evaluation criteria, required manuscript structure, data/code availability
policy, statistics/reporting standards, and anything reviewers should weigh.
"""

last_updated = "2026-06-01"

# --- Per-article-type overrides (optional) ---
# The article-type taxonomy (article, letter, communication, perspective,
# review, technical-note, tutorial) and what each means is shared across all
# venues — see peerreviewagents/article_types.py. Here you only override the
# venue-specific specifics for the types you care about: word/abstract caps
# and any handling notes. Types you don't list still get the general framing.
# Delete this section if the venue doesn't differentiate by manuscript type.
#
# [article_types.article]
# max_words = 8000
#
# [article_types.letter]
# max_words = 5500
# notes = "Presubmission inquiry recommended."

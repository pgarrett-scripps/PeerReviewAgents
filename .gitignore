__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.env
peerreview.toml
.venv/
venv/
reports/*
!reports/.gitkeep
.pytest_cache/
.ruff_cache/

# User-uploaded manuscripts (web UI drop zone)
.peerreview-uploads/

# Evaluation corpus + runs (PDFs, jsonl, reports)
data/

# Human-vs-AI benchmark: keep the scaffolding (scripts, README), ignore
# everything derived. Copyrighted PDFs/reviews and AI outputs never belonged
# here; the filled worksheets and their audits are manuscript material and
# live in the private companion repo (a private companion repo).
benchmark/corpus/
benchmark/ai_reviews/
benchmark/manifest.csv
benchmark/comparison/
benchmark/verification/
benchmark/contamination/

#!/usr/bin/env python3
# ruff: noqa: E402, I001
"""Launch the bundled MCP server from an unpacked plugin directory."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from peerreviewagents.mcp import run


if __name__ == "__main__":
    run()
